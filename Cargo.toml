[package]
name = "solveryn"
version = "0.1.0"
description = "Agent Trust Verifier for Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "solveryn"

[dependencies]
anchor-lang = "0.30.1"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[profile.release]
overflow-checks = true